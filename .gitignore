/node_modules

# Environment / secrets — NEVER commit
.env
.env.*
!.env.example
!.env.development.example
!.env.staging.example
!.env.production.example

# Local runtime artifacts
logs/
*.log
stdout.txt
stderr.txt
backups/*.archive
backups/*.gz

# Diagnostics dumps (may contain PII/secret material)
uploads/